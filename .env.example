PORT=3000
JWT_SECRET=change_me_to_a_long_random_string_at_least_64_chars
JWT_EXPIRES_IN=7d

# Local development — SQLite stored in db/ folder
DB_PATH=./db/consulting.db

# Railway production — point to the persistent volume
# DB_PATH=/data/consulting.db

NODE_ENV=development
